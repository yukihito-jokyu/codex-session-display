# Issue #178 実装計画

## 目的

セッション詳細画面のタイムラインを、本文・要約のテキスト検索、5つのイベント種別、
トークン計測有無で絞り込めるようにする。

絞り込み後も元のターン番号、疑似ターン表示、項目順を維持し、JSONL上の位置関係を
判断できるようにする。

## 公開インターフェース

`ConversationTimeline` の既存Props `turns` は変更せず、タイムライン内に次の操作UIを追加する。

- 検索入力: `aria-label="タイムラインを検索"`
- 種別チェックボックス:
  - 会話
  - 推論
  - ツール・コマンド
  - 参照情報
  - システムイベント
- トークン条件チェックボックス: `トークン計測あり`

既存DTOの `kind` は表示フィルター上で次の5群へ対応付ける。

| フィルター | `kind` |
| --- | --- |
| 会話 | `conversation` |
| 推論 | `reasoning` |
| ツール・コマンド | `tool`, `web`, `mcp` |
| 参照情報 | `instructions`, `reference` |
| システムイベント | `system` |

バックエンドDTO、Wailsバインディング、キャッシュスキーマは変更しない。

## 振る舞い

- 初期状態では検索語なし、全5種別を選択、トークン条件なしで全項目を表示する。
- 検索は大文字・小文字を区別せず、`body` と `details[].value` を対象にする。
  - 推論要約は `details` に含まれるため検索対象になる。
- 種別チェックボックスを外すと、対応する項目だけを非表示にする。
- `トークン計測あり` は `token_count_count > 0` の項目だけを表示する。
- 複数条件はANDで適用する。
- 一致項目を持つターンだけを表示し、元のターン番号・疑似ターン表示・項目順を維持する。
- 一致項目がない場合は、タイムライン内に明確な空状態を表示する。
- フィルター状態と会話以外の展開状態は画面内だけで管理し、永続化しない。

## TDDサイクル

1. トレーサー弾: 本文・要約検索
   - RED: 検索入力で本文または推論要約に一致する項目と元のターン境界だけが表示されることをE2Eで検証する。
   - GREEN: 検索入力と最小限の項目・ターン絞り込みを追加する。
2. 種別フィルター
   - RED: 5種別が初期選択され、個別に非表示・再表示できることを検証する。
   - GREEN: `kind` の5群対応とチェックボックス状態を追加する。
3. トークン計測フィルター
   - RED: `トークン計測あり` で計測項目だけが残ることを検証する。
   - GREEN: `token_count_count > 0` 条件を追加する。
4. 複合条件と空状態
   - RED: 検索・種別・トークン条件をAND適用し、不一致時に空状態が出ることを検証する。
   - GREEN: 共通の判定処理と空状態を追加する。
5. リファクタリング
   - 分類定義、検索正規化、項目判定をタイムライン内部の小さな純粋関数へ整理する。
   - 既存の展開状態、ターン表示、トークン表示との重複を確認する。

## ドキュメント

- `docs/requirements.md`: 検索対象、5種別、トークン条件、ターン境界、空状態を追加する。
- `docs/detailed-design.md`: UI状態、種別対応、AND条件、E2Eケースを追加する。
- 新規ADRは作成しない。既存DTOを使う画面内フィルタリングであり、アーキテクチャや
  永続データ構造の新しい意思決定を伴わないため。

## 検証

- 各RED/GREENで対象Playwrightテストを
  `task test:e2e:detail -- session-detail.spec.ts --grep "<対象テスト>"` で実行する。
- `frontend` で `npm run lint`
- `frontend` で `npm run build`
- 最終確認で `task test:e2e`

## 優先する振る舞い

1. 本文・推論要約を確実に検索できること
2. 5種別とトークン条件を組み合わせても元のターン境界・順序が維持されること
3. 初期状態で既存表示が変わらないこと
4. 不一致時に空状態が明確であること
