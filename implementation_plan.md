# Issue #179 実装計画

## 目的

長いタイムライン項目は先頭部分だけを表示し、必要な場合だけ既存の下部詳細パネルで
全文を閲覧・検索できるようにする。短い項目の表示と既存のキャンバス選択、
トークン詳細表示は変更しない。

## 公開インターフェース

`ConversationTimeline` に次のPropsを追加する。

- `onShowFullText(item: ConversationTimelineItem): void`

`SessionDetailMainContent` はこのイベントを画面状態へ中継する。
`useSessionDetail` はタイムライン項目の `body` と `details` から下部パネル表示用の
一時的な `FlowNode` を構築し、既存のノード詳細モードで選択する。

バックエンドDTO、Wailsバインディング、キャッシュスキーマは変更しない。
タイムラインDTOがすでに保持する全文を再利用する。

## 振る舞い

- タイムライン項目の本文と詳細を連結した表示内容が一定文字数を超える場合、先頭部分と
  省略表示だけをDOMへ描画する。
- 省略された項目だけに「全文を表示」ボタンを表示する。
- 「全文を表示」を押すと、その項目を下部詳細パネルの選択対象として開く。
- 下部詳細パネルではタイムライン項目のラベルと全文を表示する。
- 全文があるノード詳細には検索入力を表示し、大文字・小文字を区別せず一致箇所を
  強調表示する。
- 検索語が空の場合は全文を通常表示し、一致しない場合は0件と表示する。
- 短い本文・詳細には省略表示と「全文を表示」を出さない。
- タイムライン検索は従来どおり省略前の `body/details` 全文を対象にする。
- キャンバスノードまたはトークンバッジを選択した場合は、従来どおりそのノード詳細へ
  切り替える。

## TDDサイクル

1. トレーサー弾: 長文の省略と詳細パネル表示
   - RED: 長いコマンド出力が省略され、「全文を表示」で下部詳細パネルに全文が出ることを
     E2Eで検証する。
   - GREEN: プレビュー生成、ボタン、下部パネルへの選択経路を最小実装する。
2. 短文の非省略
   - RED: 短い項目には「全文を表示」が出ず、従来どおり表示されることを検証する。
   - GREEN: 省略判定を境界値込みで整理する。
3. 全文検索
   - RED: 詳細パネル内の検索で複数の一致箇所が強調され、不一致時に0件となることを
     検証する。
   - GREEN: `NodeDetail` に検索状態と一致強調を追加する。
4. 既存選択との連携
   - RED: タイムライン全文表示後にキャンバスノードを選ぶと通常のノード詳細へ
     切り替わることを検証する。
   - GREEN: 既存選択ハンドラーとの状態切替を最小調整する。
5. リファクタリング
   - 全文組み立て、プレビュー、省略判定、検索強調を小さな純粋関数へ整理する。
   - 長大な文字列をタイムラインDOMへ描画していないことを確認する。

## ドキュメント

- `docs/requirements.md`: 省略条件、全文表示、詳細検索、短文の扱いを追加する。
- `docs/detailed-design.md`: Props、表示状態、検索挙動、E2Eケースを追加する。
- 新規ADRは作成しない。既存DTOと下部詳細パネルを再利用する画面内表示変更であり、
  アーキテクチャや永続データ構造の変更を伴わないため。

## 検証

- 各RED/GREENで対象Playwrightテストを
  `task test:e2e:detail -- session-detail.spec.ts --grep "<対象テスト>"` で実行する。
- `frontend` で `npm run lint`
- `frontend` で `npm run build`
- 最終確認で `task test:e2e`
- `frontend-review` スキルで差分をレビューする。

## 優先する振る舞い

1. 長大な本文をタイムラインDOMへ常時描画しないこと
2. 「全文を表示」から既存の下部詳細パネルで欠落なく閲覧できること
3. 全文検索で一致箇所を確認できること
4. 短文と既存のノード・トークン選択を壊さないこと
