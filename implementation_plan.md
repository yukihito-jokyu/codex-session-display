# Issue #169 実装計画

## 目的

セッション詳細画面の表示・操作仕様を維持したまま、画面固有の状態と操作ロジックを
`useSessionDetail` へ集約し、`SessionDetailPage` を取得状態に応じた画面構成へ専念させる。

## 公開インターフェース

- URL、Wails IPC、表示文言、DOM 上の操作対象は変更しない。
- `SessionDetailPage` は `useSessionDetail(id)` が返す状態・イベントハンドラーを利用する。
- フックは取得状態、ノード選択、詳細モード、トークン情報、ズーム対象、分割比率、
  ログ操作結果をまとめて公開する。

## 優先する振る舞い

1. ノード、トークンバッジ、統計情報からの選択が従来どおり連携する。
2. 選択ノードに紐づくトークン詳細が表示され、分割幅をポインターと矢印キーで変更できる。
3. エラー画面からログフォルダ展開とログパスコピーを実行できる。
4. 再試行と一覧への遷移を含む既存画面仕様が変わらない。

## TDD 方針

本 Issue は公開動作を変えないリファクタリングであるため、内部構造を検証するテストは
追加しない。既存の Playwright E2E を特性テストとして利用し、以下の垂直な小ステップごとに
対象テストを実行する。

1. ベースラインとして対象 E2E が成功することを確認する。
2. 選択状態、詳細モード、トークン導出、ズーム対象をフックへ移す。
3. 分割幅とリサイズ操作をフックへ移す。
4. ログフォルダ展開とログパスコピーをフックへ移す。
5. ページから不要な状態管理・副作用・導出処理を除去する。
6. Biome とセッション詳細 E2E、最後に E2E 全体を実行する。

## ドキュメント

- 既存の要件・詳細設計・ADR が今回の責務分離方針をすでに定義しているため、新規 ADR は
  作成しない。
- 実装結果と検証結果を `walkthrough.md` に記録する。
