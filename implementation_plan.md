# Issue #180 実装計画

## 目的

左タイムライン、中央キャンバス、右側トークン統計、下部詳細パネルで同じ表示単位を
選択対象として扱う。どの領域から選択しても対応要素を同じアクセントで強調し、
タイムラインとキャンバスだけを対象位置へ移動する。

## 公開インターフェース

`ConversationTimelineItem` に次の選択関連情報を追加する。

- `selection_id`: タイムライン表示単位を一意に識別する安定ID
- `node_id`: キャンバスで代表として選択・ズームするノードID
- `node_ids`: 表示単位に紐付く全ノードID
- `token_count_indices`: 表示単位に紐付く全token_countのインデックス

バックエンドは既存の表示単位、`RecordToNodeID`、`TokenCountWithBinding` を使って
これらを確定する。キャッシュスキーマを5へ更新し、古いキャッシュは再解析する。

フロントエンドの `useSessionDetail` は選択元ごとの状態を持たず、次を含む共通選択を
管理する。

- 選択中のタイムライン表示単位ID
- 選択中の代表ノード
- 強調対象token_countインデックス集合
- 下部詳細パネルの表示モード

各コンポーネントは共通選択から必要な値だけをPropsで受け取る。

## 振る舞い

- タイムライン項目を選ぶと、その項目、代表ノード、関連する全統計行・全チャート点、
  下部詳細パネルを同時に選択状態へする。
- ノードまたはトークンバッジを選ぶと、対応タイムライン項目へ自動スクロールし、
  関連する全token_countを強調する。
- TOKEN COUNT LOG行またはLAST TOKEN CONSUMPTION PER INDEXの点を選ぶと、
  対応表示単位の全要素を選択し、キャンバスを代表ノードへズームする。
- 右側パネルは選択時にスクロール位置を変更しない。
- 同じ表示単位に複数のtoken_countがある場合、全行と全系列の対応点を強調する。
- タイムライン項目の再選択、キャンバス背景、下部詳細パネルの閉じる操作で選択を解除する。
- 選択解除時は全領域の強調を解除する。
- トークンバッジ・統計からの選択では既存のトークン詳細表示を維持する。
- 通常ノード・タイムライン項目からの選択ではノード詳細表示を維持する。
- タイムラインの展開、検索、絞り込み、全文表示は既存どおり動作する。

## TDDサイクル

1. トレーサー弾: タイムラインから全領域を選択
   - RED: タイムライン項目を選ぶと、項目、ノード、統計行、チャート点、下部詳細が
     同時に強調されることをE2Eで検証する。
   - GREEN: DTOの選択情報と共通選択状態を最小実装する。
2. 統計・ノードからの逆方向連携
   - RED: TOKEN COUNT LOG、チャート点、ノードから選択した際に、対応タイムライン項目へ
     スクロールし、キャンバスが必要時だけズームすることを検証する。
   - GREEN: 選択解決とタイムラインスクロール要求を実装する。
3. 複数token_countの同時強調
   - RED: 1表示単位に複数のtoken_countがある場合、全行と全系列の点が強調されることを
     検証する。
   - GREEN: `token_count_indices` を使った集合ベースの強調を実装する。
4. 選択解除と既存詳細操作
   - RED: 選択解除で全強調が消え、ノード詳細・トークン詳細・全文表示が維持されることを
     検証する。
   - GREEN: 共通解除処理と詳細モード切替を整理する。
5. リファクタリング
   - 選択解決を `useSessionDetail` 内の小さな公開操作へ集約する。
   - コンポーネント間で選択ロジックを重複させない。
   - 各リファクタリング後に対象テストを再実行する。

## ドキュメント

- `docs/requirements.md`: 共通選択、強調、スクロール・ズーム、複数token_countを追加する。
- `docs/detailed-design.md`: DTO、状態、Props、選択遷移、E2Eケースを追加する。
- `docs/adr/0030-shared-display-unit-selection.md`: 表示単位の選択関連情報を
  バックエンドDTOで確定し、フロントエンドで共通選択を管理する決定を記録する。

## 検証

- 各RED/GREENで対象GoテストまたはPlaywrightテストを実行する。
- `go test ./...`
- `go test -tags production ./...`
- `golangci-lint run`
- `frontend` で `npm run lint`
- `frontend` で `npm run build`
- `task test:e2e`
- `go-review` と `frontend-review` で差分をレビューする。

## 優先する振る舞い

1. どの領域から選択しても同じ表示単位へ解決されること
2. 複数token_countが欠落なく同時強調されること
3. タイムラインの自動スクロールとキャンバスの選択・ズームが正しいこと
4. 選択解除と既存のノード・トークン詳細操作を壊さないこと
