# AGENTS.md — AIエージェント開発ガイドライン

このドキュメントは、`codex-session-display` リポジトリで共同開発を行うすべての AI エージェント（Claude、Gemini など）に向けた、開発時の行動原則、利用可能なツール（ワークフロー・スキル）、およびドキュメントのライフサイクルを定義した**最優先のエントリポイント**です。

エージェントは、本プロジェクトで作業を開始する際、必ず最初にこのファイルを読み込み、その指示に従ってください。

---

## 1. プロジェクト情報の構造（リファレンスマップ）

プロジェクトの各種情報やルールは以下のファイル・ディレクトリに整理されています。作業対象やフェーズに応じて適切なドキュメントを参照してください。

- [CONTEXT.md](CONTEXT.md)
  - プロジェクトのドメイン定義、コア概念（セッション、ターン、ハーネスなど）、および JSONL レコード型の仕様が記載されています。ドメイン知識の理解に最初に使用してください。
- [docs/](docs/)
  - システムの要件定義（[requirements.md](docs/requirements.md)）、詳細設計（[detailed-design.md](docs/detailed-design.md)）、およびアーキテクチャ意思決定記録（[docs/adr/](docs/adr/)）が格納されています。
- [.agents/rules/](.agents/rules/)
  - 各開発環境における具体的なコーディング規約、アーキテクチャ構造、およびテスト方針が記載されています。

---

## 2. 開発ルールの事前参照義務

エージェントは、実装する領域に応じて、以下のルールファイルを**事前に必ず読み込み**、その規約に従って実装およびテストを行わなければなりません。

- **Go バックエンドを触る場合:**
  - [backend.md](.agents/rules/backend.md) を必ず事前に読み込んでください。クリーンアーキテクチャのレイヤー構造、エラーハンドリング、構造化ログ、C0カバレッジの担保といったルールが定義されています。
- **React フロントエンドを触る場合:**
  - [frontend.md](.agents/rules/frontend.md) を必ず事前に読み込んでください。HashRouterの強制、CSS Modulesの使用、Biomeによるコードスタイル整形、PlaywrightによるE2Eテストといったルールが定義されています。

---

## 3. 仕様・設計書のライフサイクルと ADR の義務化

エージェントは、コードを変更するだけでなく、ドキュメントの保守に対しても責任を持ちます。以下のライフサイクルルールを厳守してください。

1. **実装前の仕様把握:**
   - 開発や修正に着手する前に、必ず [requirements.md](docs/requirements.md) や [detailed-design.md](docs/detailed-design.md) を読み込み、機能の背景とゴールを正しく把握してください。
2. **意思決定記録 (ADR) の作成義務:**
   - アーキテクチャの変更、重要なデータ構造の変更、外部ライブラリの導入など、重要な設計上の決定を行う場合は、必ずエージェント自身で [docs/adr/](docs/adr/) 配下に新しい ADR（`XXXX-title.md`）を起票してその背景と決定事項を記録してください。
3. **ドキュメントの同期メンテナンス:**
   - 実装の変更に伴って仕様や設計に変更が生じた場合は、コードの修正と同一のコミット/PR内で [docs/](docs/) 配下の該当ドキュメントを更新し、常に「ドキュメントとコードの完全な同期」を保ってください。

---

## 4. ワークフローとスキルの適用判断マップ

エージェントは、自身の開発フェーズに合わせて、`.agents/` ディレクトリ内に定義されている「ワークフロー」や「スキル」を自律的かつ適切に選択・提案して実行してください。

| 開発フェーズ | 推奨ワークフロー / スキル | 説明 |
| :--- | :--- | :--- |
| **要件定義・タスク整理** | `to-prd` (スキル)<br>`to-issues` (スキル) | ユーザーの要望をPRDに落とし込み、個別のIssueやタスクに分解して計画を立てます。 |
| **設計・合意形成** | `/design-review` (ワークフロー)<br>`/grill-me` (ワークフロー)<br>`grill-with-docs` (スキル) | ユーザーと対話して設計の懸念事項を潰し、`CONTEXT.md` や ADR をインラインで更新します。 |
| **試作・検証** | `prototype` (スキル) | 本番コードを汚さず、使い捨てのプロトタイプを作成してUIやロジックの実現性を検証します。 |
| **実装・バグ修正** | `tdd` (スキル) | テスト駆動開発（Red-Green-Refactor）を用いて、品質とテストカバレッジを担保しながらコードを実装します。 |
| **コードレビュー・品質検証** | `go-review` (スキル)<br>`frontend-review` (スキル) | Goコードの差分・実装内容の目視レビュー、およびフロントエンド（TypeScript/CSS/E2Eテスト）の自動検証と目視レビューを行います。 |
| **コミット・PR作成** | `/commit` (ワークフロー)<br>`/pr` (ワークフロー) | 変更内容に基づき、適切なコミットメッセージの生成とコミットの実行、およびPRの作成を行います。 |
| **リリース・公開** | `release` (スキル) | セマンティックバージョニングに則ってGitタグを作成・プッシュし、GitHub Actionsによるリリース自動化をトリガーします。 |

---

## 5. 基本的な行動規範

- **プランニングモードの厳守:**
  - 軽微な修正を除き、大きな変更を加える前には必ず `implementation_plan.md` を作成してユーザーのレビューと承認（Approve）を得てください。
- **タスク管理の徹底:**
  - 開発作業中は `task.md` を常に更新し、現在の進捗状況を追跡可能な状態に維持してください。作業完了後は `walkthrough.md` で結果を報告してください。
- **品質の担保:**
  - 変更を加えた後は必ずリンター（Go: `golangci-lint`, Frontend: `Biome`）およびテスト（Go unit tests, Playwright E2E）を実行し、エラーのない状態を確認してからコミットを提案してください。
  - Playwright E2E の全体確認には、成功ログを抑制する `task test:e2e` を使用してください。失敗原因を調査する場合は、`task test:e2e:detail -- <対象ファイル> --grep "<テスト名>"` で対象を絞り、標準の詳細ログを表示してください。
