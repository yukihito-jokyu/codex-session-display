# Issue #178 実装結果

## 実装内容

- タイムライン本文と `details` の要約を大文字・小文字を区別せず検索できるようにした。
- 会話、推論、ツール・コマンド、参照情報、システムイベントの5種別フィルターを追加した。
- `token_count_count > 0` の項目だけを表示する「トークン計測あり」を追加した。
- 検索・種別・トークン条件をAND適用し、該当項目を持つターンだけを表示するようにした。
- 絞り込み後も元のターン番号、疑似ターン表示、項目順、展開状態を維持した。
- 条件に一致しない場合の空状態を追加した。
- 要件定義、詳細設計、E2Eモック、Playwrightテストを同期した。

## TDD

1. 本文・推論要約の検索とターン境界維持
2. 5種別の初期選択と個別表示切替
3. トークン計測済み項目への絞り込み
4. 複合AND条件と空状態
5. フィルター変更後も展開状態を維持する安定キー

各ケースで失敗を確認してから最小実装を追加し、GREENを確認した。

## 検証結果

- `npm run lint`: 成功
- `npm run build`: 成功
- `task test:e2e:detail -- session-detail.spec.ts`: 57件成功
- `task test:e2e`: 85件成功
- frontend-review: 指摘事項なし
